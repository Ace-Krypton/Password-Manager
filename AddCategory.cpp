#include <iostream>
#include <string>
#include <vector>
#include "header.hpp"

/**
 * Add Category
 * @return void
 */
auto add_category() -> void {
    std::vector<std::string> categories;  //Vector for categories
    std::string category_input;  //User input
    std::cout << "+-----------------------------------------------------------------------------------+" << std::endl;
    std::cout << "      Adds a new category to be used with creating new passwords\n" << std::endl;

}
